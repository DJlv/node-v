# 根目录文章测试

这是一篇直接放在 `docs/test/` 下的 Markdown 文件（与文件夹同级）。

## 验证点

- 首页 `test/home` 应出现 **overview** 卡片
- 左侧栏 `/docs/test/` 下应出现 **overview** 链接
- 本文不应被嵌套在任何子文件夹里

## 示例内容

```js
console.log("hello test");
```

- [ ] 首页可见
- [ ] 侧边栏可见
- [ ] 链接可正常跳转
