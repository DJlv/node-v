# 三级目录 - Alpha 页

结构：`nested/module-b/page-alpha.md`

## 层级

```
test          （一级 / 顶部导航）
  nested      （二级 / 侧边栏分组）
    module-b  （三级 / 侧边栏子项或分组）
      page-alpha  ← 当前页
```

## 预期

左侧栏应显示 **nested → module-b → page-alpha**（或与 javase 同名单文件规则一致）。
